import React from 'react';
import styled from 'styled-components';
import {Logo} from '../styles/styled-components';

const Navbar = () => {
    return (
        <>
            <Logo primary>Atomic<Logo as='span'>URL</Logo></Logo>
        </>

    )
}

export default Navbar